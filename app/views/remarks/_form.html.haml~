= form_for @remark do |f|
  =error_messages_for @remark
  
  .form-group
    = f.label :topic
    = f.text_field :topic, class: 'form-control'
  .form-group
    = f.hidden_field :user_id, @current_user.id.to_s
  .form-group
    = f.label :description
    = f.text_area :description, class: 'form-control'
  .form-action
    = f.submit 'Создать', class: 'btn btn-primary'
