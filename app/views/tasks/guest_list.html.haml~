%h4 Участники
.list-group
  -@guest_list.each do |guest|
    =link_to guest.group, class:'list-group-item' do
      =guest.group.full_name
      .text-muted
        =guest.group.email
        %br
          =guest.group_type
    .clearfix    

%br
  .col-sm-6
    
    %form{action: "search", controller: "users", role: "search"}
      .form-group
        %label Искать среди существующих пользователей
        %input#search_pattern.form-control{name: "search_pattern", placeholder: "Поиск", type: "text"}/
      =link_to 'Найти', search_path(flag: "task", task_id: @task.id), class:'btn btn-default'
  .col-sm-2
    .pull-right
      .form-group
        %br
          %br
            %label ИЛИ
  .col-sm-4  
    .pull-right
      .form-group
        %label Если такого пользователя нет
        %br   
          = link_to 'Создать новый контакт', new_user_contact_path(@current_user, data: "guest",task_id: @task.id), class: 'btn btn-primary' 