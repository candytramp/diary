= form_for @task_user do |f|
  - if @task_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@task_user.errors.count, "error")} prohibited this task_user from being saved:"
      %ul
        - @task_user.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :task
    = f.text_field :task
  .field
    = f.label :group
    = f.text_field :group
  .actions
    = f.submit 'Save'
